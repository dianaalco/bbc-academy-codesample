{
    "name": "weather-cities",
    "version": "0.0.0",
    "engines": {
        "node": ">= 4.3"
    },
    "scripts": {
        "test": "mocha --reporter mocha-tap-reporter"
    },
    "dependencies": {
        "rss-parser": "2.5.2",
        "yargs": "^6.6.0"
    },
    "devDependencies": {
        "mocha": "^3.0.2",
        "mocha-tap-reporter": "^0.1.3"
    }
}
